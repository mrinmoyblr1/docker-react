sudo: required
services:
    - docker


before_install:
    - docker build -t mrinmoybiswas/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true mrinmoybiswas/docker-react npm run test

